namespace WorkChronicle.Structure.WorkHoursByYears
{
    public class WorkHoursByYear
    {
        public static KeyValuePair<int, string[]> Year2026(int month)
        {
            Dictionary<int, string[]> monthByWorkDaysByWorkHours = new Dictionary<int, string[]>()
                {
                   //the data for the 2026 hours
                };

            return monthByWorkDaysByWorkHours.FirstOrDefault(x => x.Key == month);
        }

        public static KeyValuePair<int, string[]> Year2025(int month)
        {
            Dictionary<int, string[]> monthByWorkDaysByWorkHours = new Dictionary<int, string[]>()
            {
                //the data for the 2025 hours
            };

            return monthByWorkDaysByWorkHours.FirstOrDefault(x => x.Key == month);
        }

        public static KeyValuePair<int, string[]> Year2024(int month)
        {

            Dictionary<int, string[]> monthByWorkDaysByWorkHours = new Dictionary<int, string[]>()
            {
               //the data for the 2024 hours
            };

            return monthByWorkDaysByWorkHours.FirstOrDefault(x => x.Key == month);
        }

        public static KeyValuePair<int, string[]> Year2023(int month)
        {
            Dictionary<int, string[]> monthByWorkDaysByWorkHours = new Dictionary<int, string[]>()
            {
                //the data for the 2023 hours
            };

            return monthByWorkDaysByWorkHours.FirstOrDefault(x => x.Key == month);
        }

        public static KeyValuePair<int, string[]> Year2022(int month)
        {
            Dictionary<int, string[]> monthByWorkDaysByWorkHours = new Dictionary<int, string[]>()
            {
                //the data for the 2022 hours
            };

            return monthByWorkDaysByWorkHours.FirstOrDefault(x => x.Key == month);
        }

        public static KeyValuePair<int, string[]> Year2021(int month)
        {
            Dictionary<int, string[]> monthByWorkDaysByWorkHours = new Dictionary<int, string[]>()
            {
                //the data for the 2021 hours
            };

            return monthByWorkDaysByWorkHours.FirstOrDefault(x => x.Key == month);
        }
    }
}
